import { FormatCardNumberMaskPipe } from './format-card-number-mask.pipe';

describe('FormatCardNumberMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardNumberMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
